{
  "name": "design-system",
  "version": "1.0.0",
  "description": "Ailany's design system.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build" : "tsc",
    "format": "prettier --write ./**/*.{js,ts}",
    "lint": "tslint -p tsconfig.json"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^2.8.8",
    "tslint": "^6.1.3",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^5.1.3"
  }
}
